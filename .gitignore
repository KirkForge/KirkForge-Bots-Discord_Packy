# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Build
dist/
build/

# Data (runtime-generated, not committed)
data/pending_lore/
*.db
*.db-shm
*.db-wal
*.sqlite
.migrated_sqlite

# LLM tooling (never committed)
.claude/
CLAUDE.md

# --- Agent work files (per-session, not tracked) ---
# NOTE: AGENTS.md and state.md (repo ROOT) are TRACKED — do not gitignore them.
workplan.md
lessons.md
# Subdirectory state.md docs (pre-existing, never tracked) stay ignored:
src/bot/commands/state.md
src/bot/commands/handlers/state.md

# Security
*.pem
*.key
token.json
token_*.json
*token*.json
*.pickle
client_secret*.json
credentials.json
*.bak

# Runtime / generated config (do not commit)
data/guild_config.json
tsconfig.json
data/metrics.json
